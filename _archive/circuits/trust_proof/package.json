{
    "name": "agora-trust-circuits",
    "version": "0.1.0",
    "description": "ZK circuits for Agora Trust Protocol",
    "scripts": {
        "compile": "circom trust_main.circom --r1cs --wasm --sym -o build/ -l node_modules",
        "setup": "snarkjs groth16 setup build/trust_main.r1cs pot14_final.ptau build/trust_0000.zkey",
        "export-vk": "snarkjs zkey export verificationkey build/trust_0000.zkey build/verification_key.json",
        "prove": "node scripts/prove.js",
        "verify": "node scripts/verify.js",
        "test": "mocha test/*.test.js"
    },
    "dependencies": {
        "circomlib": "^2.0.5",
        "snarkjs": "^0.7.0"
    },
    "devDependencies": {
        "mocha": "^10.0.0",
        "chai": "^4.3.0"
    }
}